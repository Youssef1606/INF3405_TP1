
public class CLICommandDownload extends CLICommand {
	public String execute(CLIApp app) {
		//String filePath = argsGet(0);
		// transformer en octets et envoyer dans le out du Serveur
		return "";
	}
}
